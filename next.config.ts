import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "picsum.photos",
      "placehold.co",
      "plus.unsplash.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
